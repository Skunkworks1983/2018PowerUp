robot() {
    if(oi.isdown(joyport, buttonport))
        drivebase.fullforward()

    if(oi.ispressed(joyport, buttonport))
        drivebase.explode()

    oi.bindwhileheld(joyport, buttonport, new command())
}



runonemotor() {
    currentport;
    talonlist[]

    execute() {
        talonlist[currentport].set(joystick y value)

        if(oi.ispressed(joyport, buttonport))
            currentport++;
        if(oi.ispressed(joyport, buttonport))
            currentport--;

        if(currentport > 16)
            currentport = 0
        if(currentport < 0)
            currentport = 16
    }
}